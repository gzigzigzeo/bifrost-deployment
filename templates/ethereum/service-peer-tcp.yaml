apiVersion: v1
kind: Service
spec:
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: {{ .Values.ethereum.args.port }}
    targetPort: {{ .Values.ethereum.args.port }}
  selector:
    component: ethereum
metadata:
  name: ethereum-v{{ .Release.Revision }}-service-peer-tcp
